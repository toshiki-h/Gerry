language: python
python:
  - "3.6"

install:
  - pip install -r requirements.txt

script:
  - pytest --cov gerry
  
after_success:
  - bash < (curl -s https://codecov.io/bash)

branches:
  only:
    - master
